import * as anchor from '@project-serum/anchor';
import { AnchorFarm } from '../target/types/anchor_farm';
import {
  program,
  setupAll,
  wallet,
} from "./setup";

describe("here", () => {
  it("here", async () => {
    await setupAll();
  });
});
